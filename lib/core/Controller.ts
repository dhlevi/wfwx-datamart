import { Controller as TsoaController } from 'tsoa'

export class Controller extends TsoaController {
  // any required Controller bits that are global to every
  // controller in your application should go here
}
